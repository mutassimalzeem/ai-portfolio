# AI Portfolio Workspace

The production Next.js portfolio lives in `portfolio/`.

- App source: `portfolio/src/`
- Public assets: `portfolio/public/`
- Prisma schema: `portfolio/prisma/`
- Archived legacy files: `archive/workspace-artifacts/`

Vercel must use `portfolio` as its Root Directory.
